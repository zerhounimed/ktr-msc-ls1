<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        <link rel="stylesheet" href="https://bootswatch.com/4/flatly/bootstrap.min.css">
        {% block stylesheets %}{% endblock %}
    </head>
    <body>

    <!-- header-start -->
    <header>
        <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
            <a class="navbar-brand" href="#">MY MANAGER CARD</a>
            <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarColor02" aria-controls="navbarColor02" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>

            <div class="collapse navbar-collapse" id="navbarColor02">
                <ul class="navbar-nav mr-auto">
                    <li class="nav-item active">
                        <a class="nav-link" href="{{path('home')}}">Home <span class="sr-only">(current)</span></a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{path('profile')}}">My Profile</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{path('registration')}}">Enregistration</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{path('site')}}">Library</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('card_create') }}">Create a new contact</a>
                    </li>

                </ul>

            </div>
        </nav>

    </header>
    <!-- header  end -->
    <div class="container">
        {% block body %}{% endblock %}
    </div>
        {% block javascripts %}{% endblock %}
    </body>
</html>
